# 仓库内统一以 LF 存储；检出时按规则转换，避免跨平台行尾噪声
* text=auto eol=lf

# Windows 脚本保留 CRLF（部分老环境对 LF 的 .bat 解析异常）
*.bat text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf

# 二进制：禁止任何转换
*.exe   binary
*.zip   binary
*.ico   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
